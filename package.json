{
  "name": "controle-de-contas-inteligente",
  "version": "1.0.0",
  "description": "Um software de controle de contas moderno e intuitivo.",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp public/index.html dist/index.html && esbuild index.tsx --bundle --outfile=dist/bundle.js --jsx=automatic --external:react --external:react-dom/client --external:@google/genai --external:recharts --external:framer-motion"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "esbuild": "0.23.0"
  }
}