{
  "name": "tatu-financeiro",
  "version": "1.0.0",
  "description": "Um software de controle de contas moderno e intuitivo.",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/index.html && esbuild index.tsx --bundle --format=esm --outfile=dist/bundle.js --jsx=automatic --external:react --external:react-dom/client --external:recharts --external:framer-motion --external:xlsx && esbuild server.ts --bundle --platform=node --format=esm --outfile=dist/server.mjs",
    "dev": "npm run build && node dist/server.mjs",
    "start": "node dist/server.mjs",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@vercel/node": "^3.2.0",
    "esbuild": "0.23.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "@vercel/postgres": "^0.9.0",
    "cors": "^2.8.6",
    "date-fns": "^4.1.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "framer-motion": "^12.40.0",
    "lucide-react": "^0.577.0",
    "recharts": "^3.8.0",
    "xlsx": "^0.18.5"
  }
}
