{
  "name": "controle-de-contas-inteligente",
  "version": "1.0.0",
  "description": "Um software de controle de contas moderno e intuitivo.",
  "private": true,
  "scripts": {
    "build": "mkdir -p public && cp index.html public/index.html && esbuild index.tsx --bundle --outfile=public/bundle.js --jsx=automatic --external:react --external:react-dom/client --external:@google/genai --external:recharts --external:framer-motion"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "esbuild": "0.23.0"
  }
}